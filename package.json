{
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
      }, 
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint .",
    "preview": "vite preview",
    "api": "json-server db.json --port 3001"
  }
}

